<?php

declare(strict_types=1);

use Fisharebest\Webtrees\I18N;

/** @var string $default_palette */
/** @var \Fisharebest\Webtrees\Module\ModuleConfigInterface $module */
?>

<h1><?= e($title ?? I18N::translate('K-Theme settings')) ?></h1>

<form method="post" action="<?= e($module->getConfigLink()) ?>">
    <?= csrf_field() ?>

    <div class="row mb-3">
        <label class="col-sm-3 col-form-label" for="default_palette">
            <?= I18N::translate('Default palette') ?>
        </label>
        <div class="col-sm-9">
            <select id="default_palette" name="default_palette" class="form-select">
                <option value="light" <?= $default_palette === 'light' ? 'selected' : '' ?>><?= I18N::translate('Light') ?></option>
                <option value="dark" <?= $default_palette === 'dark' ? 'selected' : '' ?>><?= I18N::translate('Dark') ?></option>
            </select>
            <div class="form-text">
                <?= I18N::translate('Set the default palette for K-Theme.') ?>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <div class="col-sm-9 offset-sm-3">
            <button type="submit" class="btn btn-primary">
                <?= I18N::translate('Save') ?>
            </button>
        </div>
    </div>
</form>
